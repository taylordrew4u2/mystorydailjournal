import SwiftUI

/// Kept as a compatibility shell for older call sites. People are learned
/// privately through guided answers, not shown as diary tags.
struct PersonChipsSection: View {
    let record: DayRecord

    var body: some View {
        EmptyView()
    }
}
